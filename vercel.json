{
    "build": {
        "ignoreBuildStep": "if [ \"$VERCEL_GIT_BRANCH\" != \"main\" ] && [ \"$VERCEL_GIT_BRANCH\" != \"dev\" ]; then echo \"Skipping deployment for branch $VERCEL_GIT_BRANCH\"; exit 0; fi"
    }
}